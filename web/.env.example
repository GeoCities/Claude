# Mainnet RPC for ENS reads. The defaults work but rate-limit hard; bring your own.
VITE_MAINNET_RPC=https://eth.llamarpc.com

# Local services (defaults match the gateway and pinning packages).
VITE_GATEWAY_URL=http://localhost:8787
VITE_PIN_URL=http://localhost:8788
